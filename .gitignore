# 通用忽略规则
.DS_Store                  # macOS系统文件
Thumbs.db                  # Windows系统文件
*.swp                      # Vim临时文件
*.swo                      # Vim临时文件
*~                         # 临时文件

# 后端(backend)相关忽略
backend/__pycache__/       # Python编译缓存
backend/*.py[cod]          # Python编译文件
backend/*$py.class         # Python编译文件
backend/.env               # 环境变量配置（含密钥、数据库路径等）
backend/.env.local         # 本地环境变量
backend/instance/          # Flask实例目录（含SQLite数据库文件 market.db）
backend/.venv/             # Python虚拟环境（如果在backend目录内）
backend/venv/              # 虚拟环境另一种常见命名
backend/dist/              # 可能的打包输出
backend/build/             # 构建目录
backend/*.egg-info/        # Python包信息

# 前端(frontend)相关忽略（假设是Vue 2项目，基于Node.js）
frontend/node_modules/     # Node依赖
frontend/dist/             # Vue打包输出
frontend/.env              # 前端环境变量
frontend/.env.local        # 前端本地环境变量
frontend/.npm/             # npm缓存
frontend/.yarn/            # yarn缓存
frontend/package-lock.json # 可选（如果用yarn，可忽略此文件）
frontend/yarn.lock         # 可选（如果用npm，可忽略此文件）
frontend/.pnp/             # yarn pnp相关
frontend/.pnp.js
frontend/.idea/            # IDEA编辑器配置（如WebStorm）
frontend/.vscode/          # VS Code配置（可选，多人协作建议忽略）
frontend/*.log             # 前端日志文件

# 测试相关忽略
test/__pycache__/          # 测试代码的Python缓存
test/*.py[cod]
test/coverage/             # 测试覆盖率报告

# 其他
*.log                      # 通用日志文件
*.tmp                      # 临时文件
# 后端补充规则
backend/app/__pycache__/       # 明确忽略app目录下的Python缓存（之前的规则可能已覆盖，但更精确）
backend/instance/market.db     # 明确忽略SQLite数据库文件（如果instance目录下有其他文件也可保留原backend/instance/规则）
backend/*.log                  # 后端可能生成的日志文件（如run.py运行时的日志）
backend/.pytest_cache/         # 如果用pytest测试，会生成此缓存目录
backend/coverage/              # 代码覆盖率报告目录（如果使用coverage工具）

# 前端补充规则
frontend/.env.development      # 前端开发环境变量
frontend/.env.production       # 前端生产环境变量
frontend/.env.test             # 前端测试环境变量
frontend/.eslintcache          # ESLint缓存文件
frontend/npm-debug.log*        # npm错误日志
frontend/yarn-debug.log*       # yarn错误日志
frontend/yarn-error.log*
frontend/.pnpm-debug.log*      # pnpm错误日志
frontend/dist/                 # Vue打包后的输出目录（关键，必须忽略）
frontend/build/                # 部分Vue项目可能有的构建目录
frontend/.DS_Store             # 前端目录下的macOS系统文件（递归忽略）

# 编辑器/IDE配置（补充）
.idea/                         # 如果项目根目录有IDEA配置（如WebStorm）
.vscode/                       # VS Code配置（如launch.json、settings.json）
*.sublime-project              # Sublime Text配置
*.sublime-workspace